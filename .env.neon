# Database - Neon configuration
DATABASE_URL="postgresql://[your-neon-username]:[your-neon-password]@[your-neon-hostname]/pincatalog?sslmode=require"

# Keep any other existing environment variables
NEXT_PUBLIC_API_URL=""
# Add any other variables that were in your original .env file
